import {TeacherSearchPipe} from './teacher-search.pipe';

describe('TeacherSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TeacherSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
